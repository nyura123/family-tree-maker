{
  "name": "family-tree-maker-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:open": "vite --open",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:wisp": "node scripts/deploy-wisp.mjs",
    "deploy": "npm run build && npm run deploy:wisp"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
